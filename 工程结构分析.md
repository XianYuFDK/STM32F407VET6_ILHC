# 当前工程结构分析

根据 2026-09-10 源码及本次 28/35 电机移植整理；CAN 接口部分已按
2026-09-18 的 CAN2/PB5/PB6 当前配置更新。本文分析现有结构，不代表实机时序与机械行为已经验证。

## 当前分层与调用关系

```text
HostTools：Tkinter 上位机 / Qt 上位机
                  │ USART1 ASCII 命令 / JustFloat 遥测
Core/freertos.c：defaultTask
                  └─ debug_usart.c：调参 + 控制调度 + 心跳保护 + 遥测
                        ├─ mecanum_control.c：麦轮解算、OPS 位置 P 控制
                        │      ├─ ops.c → USART2 RX DMA
                        │      └─ zdt_x42s.c → UART4 阻塞发送
                        └─ dm_j4310.c → hcan.c → CAN2 (PB5/PB6)

后续调用方 → stepper_2835.c → hcan.c → CAN2 扩展帧
CAN2 RX → hcan.c → debug_usart.c 的 CAN_Rx_Callback
                         ├─ 标准帧 → DM 反馈
                         └─ 扩展帧 → 28/35 原始回复

Core/main.c → SoftSPI_OLED_Init：独立显示驱动（软件 SPI GPIO）
             后续显示由任务按需调用
Core：初始化与 IRQ / Drivers：HAL、CMSIS / Middlewares：FreeRTOS
```

当前结构可以支撑单任务调试和驱动复用。基础通信、设备协议、底盘算法基本分文件，但 `Hardware/` 内同时包含驱动、机械换算、控制逻辑和调试调度，不能将目录名称视为严格分层。

## 已有结构优点

- `ops`、`zdt_x42s`、`dm_j4310`、OLED 各有独立接口，新增 28/35 电机可共用 CAN 通道，不必复制 HAL 初始化。
- EIDE 与 Keil 共用源文件，本次已同步两份显式编译清单。
- 调试参数集中，遥测格式固定，串口调参和两个上位机有明确入口。
- 单一 CAN 接收钩子可按帧类型分派，当前没有引入重复 HAL 回调。
- 原始 OPS 的 m/rad 在底盘层转换为 mm/deg，单位边界已明确。

## 主要结构问题

| 优先级 | 现状与证据 | 影响与改进方向 |
| --- | --- | --- |
| 已修复 | USART1 在打包前检查 READY，DMA 忙仅跳过遥测 | 保留控制服务执行；仍约定由单一任务负责调试发送 |
| 高 | 心跳与 STOP 只控制 GOTO/DM，没有覆盖新增 28/35 | 当前没有全执行器停机入口。添加两台电机的调试动作前，需要先确认其停止协议和失联策略 |
| 中 | `DebugUsart_Send()` 承担命令处理、运动控制、模式切换、失联保护和遥测 | 显示/通信功能改动容易影响控制行为。后续可保持协议接口，将周期控制拆为独立服务函数，再决定是否独立任务 |
| 中 | CAN 接收分派位于 `debug_usart.c` | 删除调试模块也会丢失 DM 和 28/35 的接收更新。设备增多时适合将分派移到独立设备服务模块；本次保持既有入口 |
| 中 | 默认任务为处理完成后 `osDelay(20)`，UART4 发送阻塞且四条命令间各等待 1ms | 控制周期不是固定 20ms。OLED 刷新若频繁放入该循环会继续增加周期；需要实时性时使用绝对周期调度并测量执行时间 |
| 中 | 默认任务栈仅 512 字节 | 当前新增协议数据较小，但浮点格式化、OLED 显示与更多调用层叠加后应测量栈水位，不能用编译通过证明栈足够 |
| 部分修复 | CAN 发送头改为局部变量并用短临界区保护邮箱提交；接收仍为单帧缓存 | 已消除发送头共享与邮箱提交竞争；尚无统一发送队列，后续消费者仍可能错过中间回复。两帧提交不等于总线层原子送达 |
| 中 | OPS 仅解析当前 14 字节 DMA 块；UART4 未解析应答 | 定位帧错位恢复和步进电机执行确认能力有限；后续增加字节流同步、应答状态机时再扩展 |
| 低 | `SetPid` 接口实际只设置 P，历史 `mecanum_pid.c/.h` 仍在磁盘但不编译 | 阅读者容易误判为完整 PID；维护时明确接口语义，勿自动恢复历史文件 |
| 低 | 两版上位机分别维护协议解析和模拟器 | 修改命令、通道及限幅必须同步；后续可抽取公共协议模块 |

## 新增 28/35 驱动的边界

`stepper_2835.c/.h` 同时包含原始报文接口和原车单位换算包装。将 28 与 35 放在同一模块，是因为它们使用相同协议，仅 ID 和机械参数不同。`hcan.c` 继续处理帧发送，`zdt_x42s.c` 继续服务 UART4 底盘电机，`dm_j4310.c` 继续处理 DM 协议。

原工程的 2030、1200、1600、1660、44.94、3.189 等值属于旧机械标定，不能直接推定适用于新车。原回复处理只有 ID 标志，没有到位状态定义，因此新增接口返回原始回复快照，不将其命名为“完成”。没有自动回零或自动位置动作；使用者需主动调用，并检查 HAL 返回值。

## 后续演进顺序

1. 在增加自动动作前，确认每种电机停止/失联协议与机械行程。
2. 将调试命令和实际控制服务分离，使控制不依赖遥测发送函数。
3. 按实测需要处理 DMA 缓冲所有权、任务周期、栈水位和 CAN 多设备调度。
4. 业务成熟后再建立塔吊动作层；驱动只负责协议，机构参数集中配置。

本次新增驱动与现有接收分派已通过 AC5 编译和链接，未烧录、未发送实机命令。未调用的发送接口可能被链接器剔除，当前镜像大小不代表全部 API 被实际使用后的资源占用。
