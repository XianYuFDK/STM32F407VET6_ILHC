# STM32F407VET6_ILHC 架构与代码审查报告

> 历史状态说明：本报告记录 2026-09-16 审查当时的 CAN1/PA11/PA12 配置。
> 之后工程已迁移到 CAN2/PB5/PB6（1 Mbps）；下文 CAN1 字样仅保留为当时的
> 审查证据，不代表当前接线。当前配置见 `AGENTS.md` 的“当前 CAN 契约”。

- 审查对象：`E:\STM32\STM32F407VET6_ILHC`
- 审查范围：`Core/`、`Hardware/`、`Tests/`、Keil 工程配置（不含 `Drivers/`、`Middlewares/` 三方库内部实现）
- 审查方式：静态代码审查 + 配置核对 + HAL 路径追踪（本机 shell 不可用，全部通过文件读取完成，未编译、未上机）
- 依据规范：嵌入式 C 代码生成规范（类型 / 命名 / 错误处理 / 安全编码 / 回查清单 12 项）

---

## 一、工程定位

这是一台 **麦轮移动底盘 + 机械臂/升降机构** 的竞赛主控固件：

| 子系统 | 接口 | 驱动 | 角色 |
| --- | --- | --- | --- |
| 麦轮底盘 4 轮 | UART4 (PA0/PA1) 115200 | `zdt_x42s.c` | 张大头 ZDT_X42S 闭环步进，Emm 速度模式 |
| 全局定位 | USART2 + 空闲中断 + DMA1_S5 | `ops.c` | OPS 定位模块，14 字节帧 + CRC8 |
| 上位机调参/遥测 | USART1 + DMA 双向 | `debug_usart.c` | VOFA+ JustFloat 24 通道 + ASCII 命令 |
| 升降/伸缩 | CAN1 扩展帧 1Mbps | `stepper_2835.c` | 35 号升降、28 号伸缩 |
| 关节电机 | CAN1 标准帧 | `dm_j4310.c` | 达妙 DM-J4310-2EC |
| CAN 公共层 | CAN1 标准/扩展帧 | `hcan.c` | 分包发送 + FIFO0 接收 |
| 显示 | 软件 SPI (PB13/PC3/PE2/PE3/PE4) | `OLED_SoftSPI.c` | 128×64，仅 `main()` 用一次 |

调度：**FreeRTOS（Kernel 10.3.1，CMSIS-RTOS2 封装，ARM_CM4F 端口，heap_4）**

---

## 二、整体结论

> **一句话**：驱动层（尤其 CAN / 步进 / OLED）写得相当扎实，注释是全工程最大的亮点；但**架构层存在一个根本性问题——引入了 FreeRTOS，却没有真正使用它**。全系统只有 1 个任务、0 个队列、0 个信号量、0 处 `FromISR`，所有中断只往全局变量里写，任务靠 20ms 轮询去"捞"。

由此派生出三条主要风险链：

1. **实时性不可控**：中断里做完整 ASCII 解析；任务里做 4×阻塞串口发送 + 4ms 忙等 + 100ms 使能等待，全部压在同一个 20ms 循环。
2. **并发一致性靠运气**：跨模块全局变量被中断写、任务读，仅个别位置加了临界区。
3. **失效不可观测、不可自恢复**：无看门狗、CAN 错误完全静默、栈溢出无检测、`Error_Handler` 只是关中断死循环。

**同时必须肯定的三点**：中文注释大量在解释"为什么"与"副作用/边界"（远超一般竞赛代码水平）；`malloc/free/VLA` 为 0，缓冲区固定、环形队列、饱和计数、指针校验齐备；`Tests/hardware/` 有主机端回归测试。这说明作者具备工程意识，问题主要出在**分层与调度设计**，而不是编码态度。

---

## 三、架构图

见随本报告的架构示意图。要点：

- **中断域**：USART1(命令解析) / USART2(OPS 帧解析) / UART4(逐字节应答滑窗) / CAN1(RX FIFO0) / DMA×3 / TIM7(HAL 时基) / SysTick(RTOS 时基)。全部优先级 = 5（CAN、DMA、USART）或 15（TIM7、PendSV）。
- **任务域**：只有 `defaultTask`，栈 512 B，优先级 Normal，`for(;;) { DebugUsart_Send(); osDelay(20); }`。
- **共享数据**：中断与任务之间通过一批**裸全局变量**通信（无队列、无锁、无 `volatile` 标注的也很多）。

---

## 四、问题清单

### S1 严重（建议优先处理）

#### S1-1 中断服务程序里做完整 ASCII 命令解析

`DebugUsart_RxEventCallback()`（USART1 DMA-IDLE 中断）在中断上下文中直接调用 `Debug_ParseLine()`，其内部执行：

- 最多 64 字符的逐字节扫描与 `strchr()` 查找；
- 自研浮点/无符号列表解析（`Debug_ParseFloat`、`Debug_ParseFloatList`、`Debug_UnsignedList`、`Debug_ParseOffset`）；
- 7 项参数表线性查找（`Debug_StrCaseCmp`）或 7 路 DM 参数 `strcmp` 链；
- 向 `s_manual_velocity[]`、`s_goto_x/y/z`、`s_zdt_args[]`、`s_stepper_req[]`、`mKpx/XYVmax…` 等**跨模块全局**写入；
- 分支里还会 `Debug_ZdtAck()`（内部关/开中断）。

后果：ISR 执行时间随主机命令长度线性增长且无上界（WCET 大），拉长所有同优先级中断（USART2/CAN1/DMA）的响应延迟；在 20ms 周期里主机若连续下发命令，OPS 采样和 CAN 接收都会被推迟。

> 说明：代码里已经把"UART4 阻塞发送"挪到任务里了，方向是对的，但**解析这一半留在了中断里**，属于改造未完成。

**建议**：ISR 只做搬运（把 `s_rx` 整包拷入环形缓冲，或置 `s_rx_len` + `xTaskNotifyGiveFromISR`），解析全部移到命令任务。

#### S1-2 单任务 + 512 字节栈 + 无栈溢出检测

```c
const osThreadAttr_t defaultTask_attributes = {
  .name = "defaultTask",
  .stack_size = 128 * 4,          /* 512 字节 */
  .priority = (osPriority_t) osPriorityNormal,
};
```

这一个任务要承载的局部变量与调用深度包括：`DebugUsart_Send()` 的 `float data[24]`（96 B）、`DmJ4310Feedback_t dmFb`、`MecanumControl_GotoOPS()` 的 6 个 float + `chassis_move()` 的 `speed[4]`/9 个 float、`strlen/memcpy/memset` 调用帧，以及 `Debug_ServiceWheel() → MecanumControl_Enable() → ZDT_X42S_Enable() → HAL_UART_Transmit()` 这条较深的调用链。

同时：

- `FreeRTOSConfig.h` **未定义 `configCHECK_FOR_STACK_OVERFLOW`**（默认 0，检测关闭）；
- `vApplicationStackOverflowHook()` 只有 `cmsis_os2.c` 里的空弱实现（`__WEAK void … { }`），未在用户代码里覆盖；
- 未定义 `vApplicationMallocFailedHook`，`configUSE_IDLE_HOOK` 也为 0。

后果：栈溢出会**静默破坏相邻内存**（heap_4 的 ucHeap 或另一个 TCB），表现为随机跑飞，且没有任何钩子能捕获。

**建议**：栈提到 1 KB 以上；开启 `configCHECK_FOR_STACK_OVERFLOW=2`；实现 `vApplicationStackOverflowHook`（点 LED / 打印 / 记录到 Flash）。

#### S1-3 控制周期被阻塞式收发严重拉长

**`SetMotorVoltageAndDirection()` 单次开销约 7 ms：**

```c
for (i = 0U; i < 4U; ++i)
{
  ZDT_X42S_SpeedAcc(motor_addr[i], dir, rpm, 0U);   /* HAL_UART_Transmit 阻塞，超时 100ms */
  HAL_Delay(1U);                                     /* 每条命令忙等 1ms */
}
```

**`MecanumControl_Enable()` 再多 100 ms：**

```c
ZDT_X42S_Enable(1U); … ZDT_X42S_Enable(4U);
HAL_Delay(100U);
```

而这些函数位于 20ms 周期的关键路径上：`Debug_ServiceManual()`、`Debug_ServiceWheel()`、`MecanumControl_Stop()`、`MecanumControl_GotoOPS()` 都会触发。一次 `WHEELEN` / `WHEELOFF` 命令会让该任务阻塞约 **110 ms**，期间 OPS 位置闭环、遥测、命令处理全部停摆（名义 50 Hz 的环路瞬时掉到 <10 Hz）。

最坏估算：4 × 100 ms（串口超时） + 4 ms ≈ **400 ms 阻塞**，且此时中断仍然开着，不影响中断实时性，但任务完全失去响应能力。

**建议**：把"每条命令间隔 1 ms"改成基于 `HAL_GetTick()` 的非阻塞节拍状态机（每周期只发 1 帧）；`HAL_Delay(100)` 改成延时状态机；`HAL_UART_Transmit` 的超时从 100 ms 收紧到 ~5 ms（8 字节 @115200 只需 0.7 ms）。

#### S1-4 `numerical_limit` 施加在分解项上 → 限幅与最小速度补偿同时失效

```c
/* mecanum_control.c: chassis_move() */
vy1 = cosf(...) * mKpy * devy;  numerical_limit(&vy1, XYVmax, XYVmin, 5.0f);
vx2 = sinf(...) * mKpy * devy;  numerical_limit(&vx2, XYVmax, XYVmin, 5.0f);
vy2 = sinf(...) * mKpx * devx;  numerical_limit(&vy2, XYVmax, XYVmin, 5.0f);
vx1 = cosf(...) * mKpx * devx;  numerical_limit(&vx1, XYVmax, XYVmin, 5.0f);
...
speed[0] = (int)-(vy1 - vy2 + vx1 + vx2 + vz);
```

**先说结论：车体系变换本身是正确的**（`vx1+vx2 = cosθ·devx+sinθ·devy = ex_body`，`vy1-vy2 = cosθ·devy-sinθ·devx = ey_body`，与标准旋转矩阵一致）。

但 `numerical_limit()` 同时承担了三件事：死区（5.0）、**最小速度补偿**（`*value += min`）、最大值限幅。它被逐个施加在 4 个分解项上，于是：

- **最小速度补偿加了两次**：合成后相当于 `2 × XYVmin`（默认 10 mm/s），死区附近会有无法平滑收敛的速度跳变；
- **最大速度限幅形同虚设**：合成后最大可达 `2 × XYVmax`。上位机 `XVMAX=1600` 想限制 1600 mm/s，实际输出上限约 3200 mm/s。

**建议**：先算出 `ex_body / ey_body`，再对合成量各调一次 `numerical_limit`；把"死区 + 最小补偿"和"限幅"拆成两个职责单一的函数。

### S2 中等

| 编号 | 问题 | 说明与影响 |
| --- | --- | --- |
| S2-1 | **跨模块全局变量无同步，且非 static** | `mKpx/mKpy/mKpz/XYVmax/ZVmax/SpeedTarget[]/pos_x/pos_y/zangle/devx/devy/devz` 全在 `mecanum_control.h` 里 `extern` 暴露，中断改、任务读，只有零散几处加了临界区（`ops.c` 做得较好，`debug_usart.c` 部分做了）。典型竞态：`chassis_move()` 计算过程中连续多次读 `mKpx`，一条 `KPX=` 命令可能在本次计算中途生效 → 输出一帧混合参数结果 |
| S2-2 | **多字段状态量读到"半新半旧"组合** | `s_goto_x/y/z`（3 个 float）在 `DebugUsart_Send` 中**无临界区**依次读取，期间新 `GOTO=` 到达即产生"新 X + 旧 Y"的目标；`Debug_ParseStepper()` 在中断里写 `s_stepper_req[i]` 的 5 个字段且不关中断，任务侧关中断读 → 可能读到"旧 position + 新 speed"。`s_zdt_args[]`/`s_manual_velocity[]` 因为"先写数据、最后写标志位"而基本安全，说明作者已有此意识，但未系统化 |
| S2-3 | **CAN 错误完全静默、无 bus-off 恢复** | `CAN_Start()` 只激活 `CAN_IT_RX_FIFO0_MSG_PENDING`，未激活任何 `CAN_IT_ERROR_*`；用户代码未实现 `HAL_CAN_ErrorCallback`。总线错误 / error-passive / bus-off 时固件一无所知，`HCan_Submit()` 只会持续返回 `HAL_BUSY`/`HAL_ERROR`，现象是"电机突然不响应"，既无法诊断也无法自恢复。CAN 接插件抖动在赛场上高发 |
| S2-4 | **无看门狗** | 全工程 0 处 IWDG/WWDG 配置。配合"HardFault/NMI/Error_Handler 全是 `while(1)`"的现状，跑飞即彻底死机。对上台运行的机器人属于必须补齐项 |
| S2-5 | **关键标定参数不落 Flash** | OPS 安装偏心（`s_mount_x_mm=-50.0f, s_mount_y_mm=60.0f`）、`mKpx/mKpy/mKpz/XYVmax/ZVmax` 全部是 RAM 变量，代码注释也自陈"未写入Flash"。每次上电都要靠上位机重新下发，中途复位 = 标定全丢 |
| S2-6 | **`Error_Handler()` / `configASSERT` 无诊断** | 都是 `__disable_irq(); while(1){}`。OLED 和 USART1 此时其实已可用（`main()` 里已初始化 OLED），完全可以在错误处理器里画/发一个错误码。现在出错看不到任何现象 |
| S2-7 | **OPS 帧接收无重同步机制** | `HAL_UARTEx_RxEventCallback` 仅当 `Size == 14` **且**首字节匹配时才解析，否则只累加 `error_count`。`s_rx_buf` 只有 14 字节、DMA 为 `DMA_NORMAL`，一旦噪声把帧切开或两帧粘连，会**持续错帧且无法自行重新同步**（对比 `zdt_x42s.c` 的逐字节滑窗重同步，OPS 侧缺了同样的机制） |
| S2-8 | **`OPS_GetData()` 返回内部结构体指针** | 任务可直接读被 ISR 写入的 `s_ops`，返回 `const` 也不阻止读取撕裂。当前无人调用，属"埋雷型"接口 |

### S3 轻微 / 编码规范偏差

| 编号 | 规范项 | 现状 |
| --- | --- | --- |
| S3-1 | 命名规范 | 函数风格混用：`MecanumControl_Init()`、`OPS_GetPosition()`（PascalCase+驼峰）与 `chassis_move()`、`numerical_limit()`、`SpeedTarget_stop()`（snake_case）并存；**全局变量全无 `g_` 前缀**；缩进 `stepper_2835.c` 4 空格、其余 2 空格 |
| S3-2 | **魔数（规范第 12 条 / 常见错误 2）** | 协议字节全是裸字面量：`zdt_x42s.c` 的 `0xF3/0xAB/0xF6/0xFE/0x98/0x6B`；`dm_j4310.c` 的 `0xFC/0xFD/0xFE/0x55`、`data[3]=10U`（RID）、`0x07FF`、位打包掩码 `0x0F`；`stepper_2835.c` 的 `0x9A/0x02`。换算常数散落：`0.238f`（出现两次，互为倒数却各写一遍）、`speed*30U`、`travel*4494U/100U`、`travel*3189U/1000U`、`MECANUM_RAD_TO_DEG`、`3.1415926f` |
| S3-3 | public 函数错误处理 | 大量 `void` 且无 NULL 校验：`ZDT_X42S_Enable/Disable/Stop/Speed/SpeedAcc`、`MecanumControl_*`、`chassis_move`、`numerical_limit`、`OPS_Init`。`ZDT_X42S_SpeedAcc()` 超范围只做**静默钳位**，调用方无法得知命令未按预期下发。**正面**：CAN 与 stepper 层返回 `HAL_StatusTypeDef` 且文档化了 `HAL_OK`/`HAL_BUSY`/`HAL_ERROR` 语义，做得很好 |
| S3-4 | 类型规范 | 基本合格，但 `int`/`int16_t`/`uint16_t` 混用：`SpeedTarget[4]` 是 `int`，`s_manual_velocity[3]` 是 `int16_t`，`Debug_ParseManual` 内部用 `int` 再强转 |
| S3-5 | Doxygen 头 | `.c` 基本都有；`.h` 多数有但格式不统一，缺 `@author`/`@date` |
| S3-6 | 中文注释 | **合格且优秀**。多处解释"为什么"而非"做什么"，例如 `MecanumControl_ClearTarget` 解释"失能后下发速度帧会让驱动器重新锁轴"、`HCan_Submit` 解释临界区目的、`stepper_2835.c` 明确写出"原车注释中的机械范围和这里的实际限幅不完全一致，必须以新机构标定为准" |

### 关键安全编码项（合格，值得保留）

- `malloc/free/calloc/realloc` **0 处**；VLA **0 处**；全部使用固定大小缓冲区。
- 环形队列实现正确：`zdt_x42s.c` 的 `s_reply_queue[16][4]`（牺牲 1 槽，注释准确写明"最多缓存 15 帧"）、`debug_usart.c` 的 `s_ack_queue[16]` + 事件码。
- 饱和计数：`stepper_2835.c` 的 `if (count != UINT32_MAX) ++count;` 防止回绕误判。
- 指针 NULL 校验、参数范围校验、`Debug_ParseUnsignedList` 的溢出检测（`value > (UINT32_MAX - digit)/10U`）都很到位。
- 临界区一律采用 `primask = __get_PRIMASK(); __disable_irq(); … if (primask == 0U) __enable_irq();` 的正确可嵌套模式。
- `ops.c` 对定位数据做 NaN / Inf / 越界三重过滤（`frame.x == frame.x`、`fabsf(x) < 1000.0f`），考虑周到。

### 死代码与未使用接口（数量偏多）

| 项 | 状态 |
| --- | --- |
| `mecanum_pid.c/.h` | **整个模块 6 个函数无人调用** |
| `MecanumControl_IsNearTarget/IsInTarget/MoveTo/SetYawPid` | 无人调用 |
| `OPS_GetData/GetAbsolutePosition/IsNew/ClearNew/SetOrigin/ClearZero` | 无人调用（`ZERO` 命令走的是 `OPS_ZeroCoordinates`） |
| `HCan_GetRxFrame/HCan_ClearRxFlag` | 无人调用，`hcanRxFrame/hcanRxFlag` 因此只有 ISR 单向写 |
| `ZDT_X42S_Speed`、`Stepper2835_GetReply` | 无人调用 |
| `ZVmin` | **被定义、被赋默认值、被注册进 `s_params`（用户可发 `ZVMIN=` 修改），但控制律从未使用** → 一个"调了没反应"的假参数，比不提供更糟 |
| `MX_TIM6_Init()` | 完整初始化（Prescaler=83, Period=65535）但从不启动、从不使用 |
| `COMM_LED`(PB2) / `VM_EN` / `CAMERA_LIGHT_EN` / `START_KEY1/2` | 仅在 `MX_GPIO_Init()` 中配置，运行期 0 次读写；但 `调试指令手册.md` 仍有"PB2 LED手动控制"章节 → **文档已漂移** |
| `SoftSPI_OLED_*` | 仅在 `main()` 里显示一次 `READY`，无任务刷新；`ScrollDisplay()` 注释要求"由任务周期调用"却无人调用 → OLED 实用价值为零 |
| `TIM1`（夹爪舵机预留） | Prescaler=1679 / Period=65535 → 168MHz/1680/65536 ≈ **1.5 Hz**、脉宽 655 ms，且从不 `HAL_TIM_PWM_Start()`。即使启动也驱动不了舵机（需 50 Hz / 0.5–2.5 ms）。属"看似预留、实际不可用"的配置 |

---

## 五、两处"当前安全但很脆"的写法（建议主动加固）

这两处我实际追到了 HAL 源码，**目前不会出问题**，但成因是巧合而非设计——一旦有人改动相邻配置就会变成硬死锁，因此单列说明。

### 5.1 临界区包裹了内部含自旋等待的 HAL 调用

涉及两处：

- `ZDT_X42S_ServiceRx()`：在 `__disable_irq()` 内调用 `HAL_UART_AbortReceive()`
- `Debug_ServiceSteppers()`：在 `__disable_irq()` 内调用 `Motor_Homing()/Motor35_AbsPosition()/Motor28_AbsPosition()` → `Can_SendCmd()` → `HAL_CAN_AddTxMessage()`

为什么现在安全：

1. **UART4 没有配置 RX DMA**（`usart.c` 中只对 USART1/USART2 做了 `__HAL_LINKDMA(..., hdmarx, ...)`），因此 `HAL_UART_AbortReceive()` 中 `if (HAL_IS_BIT_SET(CR3, USART_CR3_DMAR))` 分支被跳过，不会走到 `HAL_DMA_Abort()`。
2. CAN 发送前已预检空闲邮箱数（`HCan_Submit` 检查 `GetTxMailboxesFreeLevel() == 0U`、`Motor_AbsPosition` 要求 `>= 2U`），因此 `HAL_CAN_AddTxMessage()` 不会进入"等待空闲邮箱"的路径。

为什么危险：`HAL_DMA_Abort()`（`stm32f4xx_hal_dma.c:544`）内部是

```c
while ((hdma->Instance->CR & DMA_SxCR_EN) != RESET)
{
  if ((HAL_GetTick() - tickstart) > HAL_TIMEOUT_DMA_ABORT) { …return HAL_TIMEOUT; }
}
```

而 **`HAL_GetTick()` 依赖 TIM7 中断推进（`TICK_INT_PRIORITY = 15`），TIM7 已被本地 `__disable_irq()` 屏蔽** → `tickstart` 永不推进，超时永不满足 → **死循环**。

也就是说：只要有人给 UART4 打开 RX DMA，或去掉 CAN 发送前的邮箱预检，固件会直接挂死在这两个位置之一。

**建议**：把临界区收窄到只保护标志位/指针的读写，把阻塞型 HAL 调用移到临界区之外（`DebugUsart_ServiceRx` 就是这么做的，可直接照搬其模式）。

### 5.2 回调注册机制混用

- **USART2（OPS）** 依赖 ops.c 对**全局弱符号** `HAL_UARTEx_RxEventCallback` 的覆盖；
- **USART1（调试）** 走 `HAL_UART_RegisterRxEventCallback()` 注册专用回调；
- **UART4（ZDT）** 走 `HAL_UART_RegisterCallback(RX_COMPLETE/ERROR)`。

三种方式并存。我在 `stm32f4xx_hal_uart.c:1024` 确认了 `HAL_UART_Init()` 中 `huart->RxEventCallback = HAL_UARTEx_RxEventCallback;` 的默认赋值，且 IRQ 处理器在 `USE_HAL_UART_REGISTER_CALLBACKS==1` 时调用函数指针（回落到弱符号）。**因此 USART2 的 OPS 接收是正常工作的，不是缺陷。**

但这是"隐式约定优于显式"：读代码的人必须知道 `USE_HAL_UART_REGISTER_CALLBACKS=1` 且必须确认默认赋值存在，才能确信 OPS 会收到数据。建议统一为显式注册。

---

## 六、13 项回查清单对照

| # | 检查项 | 结论 |
| --- | --- | --- |
| 1 | 是否有对应 reg.h | 不适用（使用 HAL），但协议常量应有命名宏 → **不合格** |
| 2 | 寄存器用 `volatile uint32_t` 结构体 | 不适用 |
| 3 | 位字段有 MASK/SHIFT 宏 | 部分不合格：`dm_j4310.c` 位打包用裸掩码（`0x0F`、`>>4`、`>>8`） |
| 4 | 类型仅用 stdint.h | 基本合格，`int`/`int16_t` 混用（见 S3-4） |
| 5 | 命名符合规范表 | **不合格**（见 S3-1） |
| 6 | public 函数有错误处理 | **不合格**：大量 `void`（见 S3-3） |
| 7 | 无 malloc/free/VLA | ✅ **合格** |
| 8 | 注释使用中文 | ✅ **合格且优秀** |
| 9 | 文件头 Doxygen 完整 | 部分合格（缺 `@author`/`@date`，格式不统一） |
| 10 | 生成代码符合规范 | — |
| 11 | 功能流程保持一致 | — |
| 12 | 寄存器/常量赋值用宏、禁止字面值 | **不合格**（见 S3-2） |

---

## 七、改造建议（按性价比排序）

### 第 1 步：让 FreeRTOS 真正发挥作用（收益最大、改动可控）

1. **ISR 瘦身**：`DebugUsart_RxEventCallback` 只做搬运 + `xTaskNotifyGiveFromISR`，`Debug_ParseLine` 移到命令任务。这是单点收益最大的改动。
2. **拆任务**（建议三个）：

| 任务 | 优先级 | 周期 | 职责 |
| --- | --- | --- | --- |
| `ctrlTask` | 最高 | 5–10 ms | OPS 采样 → 位置环 → 轮速下发 → 电机应答排空。发送用非阻塞分帧状态机 |
| `cmdTask` | 中 | 事件驱动 | ASCII 解析、参数变更、GOTO/MANUAL/ZDT/DM/WHEEL 状态机 |
| `teleTask` | 低 | 20–50 ms | VOFA 遥测 + **把 OLED 用起来**（状态/坐标/错误码） |

3. **参数同步用"事件队列"而非裸全局**：把 `KPX=3.0` 解析成一条 `param_change_t` 消息投递给控制任务，控制任务在周期边界统一应用，天然消除 S1-4 与 S2-1/S2-2 的竞态。

### 第 2 步：补齐可靠性与可观测性

- 加 IWDG，喂狗点放在 `ctrlTask`（只有主控环正常才喂狗）。
- 开启 `configCHECK_FOR_STACK_OVERFLOW=2`，实现 `vApplicationStackOverflowHook`（点 LED / 写 Flash / OLED 打错误码）。
- 实现 `HAL_CAN_ErrorCallback` 并激活 `CAN_IT_ERROR_WARNING | ERROR_PASSIVE | BUSOFF | LAST_ERROR_CODE`，bus-off 后走 `HAL_CAN_Start()` 自动恢复。
- `Error_Handler()` 至少点亮 LED 或写一个错误码寄存器（可用备份寄存器 `RTC->BKPxR`），别只 `while(1)`。
- OPS 侧加逐字节滑窗重同步（照搬 `zdt_x42s.c` 的做法）。

### 第 3 步：修正控制语义

- `numerical_limit` 只作用于合成后的 `vx_body / vy_body`（修 S1-4）。
- `MecanumControl_GotoOPS()` 去掉"改写全局 `XYVmax/ZVmax`"的副作用，改成显式接口；`0.238f` 定义成一处宏（目前两处互为倒数，改一处必错）。
- `chassis_move()` 的 `devz` 归一化改用 `fmodf`；补 Z 轴入参限幅（当前 `GOTO` 只限 X/Y）。
- 去掉 `DebugUsart_Send()` 里通过指针写 `pos_x/pos_y/zangle` 的做法，同时注意它每 20 ms 调一次 `OPS_GetPosition()` 会**清除 `s_new_flag`**，导致 `OPS_IsNew()` 对任何其他调用者永远返回假。

### 第 4 步：清理

- 删掉或明确标注"预留"：`mecanum_pid.*`、`ZVmin`（或接进控制律）、`MX_TIM6_Init`、`HCan_GetRxFrame/ClearRxFlag` 等。
- 修正 `TIM1`：若要驱动夹爪舵机，应配 50 Hz（Prescaler/Period 重算）并调用 `HAL_TIM_PWM_Start()`。
- 同步 `调试指令手册.md`（删除已不存在的 PB2 LED 章节）。
- 把协议字节与换算系数集中到一个 `*_protocol.h` / `*_config.h`，消除魔数。

---

## 八、附：本报告已核实的"疑似问题但实际不是"

为避免后续重复排查，列出三处曾引起怀疑、经追踪源码后确认**无问题**的点：

1. **`configENABLE_FPU 0` 会不会丢失 FPU 上下文？** —— 不会。本工程使用官方 FreeRTOS `portable/RVDS/ARM_CM4F/port.c`，其中 `prvEnableVFP()` 与 `portASPEN_AND_LSPEN_BITS` 的设置是**无条件执行**的（port.c:390-393），不受 `configENABLE_FPU` 影响（该宏属于 ST CMSIS-FreeRTOS 分支，此处为无效果的残留配置）。Keil 工程为 `FPU2`（单精度硬件 FPU），启动文件已使能 FPU。
2. **`ZDT_X42S_ServiceRx()` 关中断下 `HAL_UART_AbortReceive()` 会不会死锁？** —— 目前不会。UART4 未配 RX DMA，`HAL_UART_AbortReceive()` 跳过 DMA 中止分支。**但成因是巧合**，见第五节 5.1。
3. **`HAL_UARTEx_RxEventCallback` 是全局弱符号覆盖，而 `USE_HAL_UART_REGISTER_CALLBACKS=1` 时 IRQ 走函数指针，OPS 会不会收不到数据？** —— 不会。`HAL_UART_Init()`（`stm32f4xx_hal_uart.c:1024`）会把未注册的 `RxEventCallback` 默认赋为 `HAL_UARTEx_RxEventCallback`，因此 USART2 会落到 ops.c 的实现。

---

*审查完成时间：2026-09-16 · 静态审查，未编译、未上机，实机行为（CAN 仲裁、SPI 时序、机械行程标定）仍需台架验证。*
